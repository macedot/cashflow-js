{
  "name": "cf-sim",
  "version": "1.0.0",
  "description": "Cashflow Simulator — client-side cashflow simulation SPA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}
